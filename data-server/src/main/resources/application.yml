server:
  port: 8100
spring:
  application:
    name: "data-server"
eureka:
  client:
    serviceUrl:
      # it connects to http://localhost:8761/eureka/ by default
      defaultZone: "http://localhost:8761/eureka/"
    healthcheck:
      enabled: true

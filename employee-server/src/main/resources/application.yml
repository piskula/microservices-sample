server:
  port: 8300
spring:
  application:
    name: "employee-server"
eureka:
  client:
    serviceUrl:
      # it connects to http://localhost:8761/eureka/ by default
      defaultZone: "http://localhost:8761/eureka/"
    healthcheck:
      enabled: true
